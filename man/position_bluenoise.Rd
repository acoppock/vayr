% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/position-bluenoise.R
\name{position_bluenoise}
\alias{position_bluenoise}
\title{Scatter over-plotted points evenly at random}
\usage{
position_bluenoise(width = NULL, height = NULL, candidates = 10, seed = NA)
}
\arguments{
\item{width, height}{The dimensions of the elliptical field the points are
spread across.}

\item{candidates}{The number of random draws considered for each point. Larger
values space the points more evenly and take longer. The default of 10 is
enough to remove essentially all of the clumping.}

\item{seed}{A random seed for reproducibility.}
}
\value{
A \code{ggproto} object of class \code{PositionBlueNoise}.
}
\description{
This function spreads perfectly over-plotted points across an elliptical
field, like \code{\link[=position_jitter_ellipse]{position_jitter_ellipse()}}, but places them so that no two land
much closer together than the rest. Sampling uniformly at random, which is
what jittering does, leaves visible knots and voids: in a draw of 200 points
the closest pair typically sits about a fifteenth of the median spacing
apart. A reader cannot tell those knots from real structure. The arrangement
here has the even spacing of \code{\link[=position_sunflower]{position_sunflower()}} while still looking
unstructured, so no reader mistakes a spiral arm for a finding.
}
\details{
The pattern is the one the eye's own photoreceptors are laid out in, known as
blue noise or a Poisson-disc distribution. It is produced by Mitchell's
best-candidate algorithm: each point is the best of \code{candidates} random draws,
where best means farthest from every point already placed.
}
\examples{
  library(ggplot2)

  dat <- data.frame(x = rep(1, 400), y = rep(1, 400))

  # Evenly scattered.
  ggplot(dat, aes(x, y)) +
    geom_point(position = position_bluenoise(width = 0.5, height = 0.5)) +
    coord_equal(xlim = c(0, 2), ylim = c(0, 2))

  # Uniformly jittered, for comparison. Note the knots and the gaps.
  ggplot(dat, aes(x, y)) +
    geom_point(position = position_jitter_ellipse(width = 0.5, height = 0.5)) +
    coord_equal(xlim = c(0, 2), ylim = c(0, 2))

}
\seealso{
Other Functions:
\code{\link[=impute_extreme_values]{impute_extreme_values()}},
\code{\link[=position_bluenoisedodge]{position_bluenoisedodge()}},
\code{\link[=position_circlepack]{position_circlepack()}},
\code{\link[=position_circlepackdodge]{position_circlepackdodge()}},
\code{\link[=position_honeycomb]{position_honeycomb()}},
\code{\link[=position_honeycombdodge]{position_honeycombdodge()}},
\code{\link[=position_jitter_ellipse]{position_jitter_ellipse()}},
\code{\link[=position_jitterdodge_ellipse]{position_jitterdodge_ellipse()}},
\code{\link[=position_sunflower]{position_sunflower()}},
\code{\link[=position_sunflowerdodge]{position_sunflowerdodge()}},
\code{\link[=sunflower]{sunflower()}}
}
\concept{Functions}
